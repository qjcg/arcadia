# Elevator Pitch

For developers using monorepos who need to manage independent versions
for multiple internal modules, sv is a semantic versioning tool that
provides automated tag calculation and management. Unlike
caarlos0/svu, our product supports path-based tagging (e.g.,
`x/slidesdeck/v2.3.1`) to enable modular versioning within a single
repository.
