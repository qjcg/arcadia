env HOME=$WORK/home
mkdir $WORK/home

# Initialize
skillo init

# Create a skill dir manually
mkdir -p $HOME/.agents/skills/test-skill

# List shows the skill (orphaned since not in selections)
skillo list
stdout 'test-skill'

# Remove the skill (by name) — test command exists
skillo --help
stdout 'remove'