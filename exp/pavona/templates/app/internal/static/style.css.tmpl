/* Custom styles for {{.project_name}} */
/* Tailwind and DaisyUI are loaded via CDN */

.hero {
    background: linear-gradient(135deg, var(--fallback-bc, oklch(var(--bc)/0.05)) 0%, transparent 50%);
}
