#+TITLE: Slidesdeck Expanded Overview
#+OPTIONS: toc:nil

* Welcome to Slidesdeck
/Plain text, polished slides./

Slidesdeck is a single-purpose CLI tool that transforms your notes into professional, self-contained HTML slideshows.

- Type @@html:<kbd>n</kbd>@@ or @@html:<kbd>p</kbd>@@ to navigate
- Type @@html:<kbd>?</kbd>@@ to see all help
- Type @@html:<kbd>/</kbd>@@ to search slides
- Type @@html:<kbd>t</kbd>@@ to change color themes
- Type @@html:<kbd>T</kbd>@@ to change font themes

-----

* Core Philosophies
** Simplicity
- No complex configuration
- One command to convert
** Portability
- Single HTML file output
- Zero external CSS/JS dependencies (all embedded)
** Developer Experience
- Native Go performance
- Clear errors and fast feedback loop

-----

* Multi-Format Support
Slidesdeck natively supports both:
1. *Org-mode* (.org)
2. *Markdown* (.md)

Both formats use standard separators:
- First-level headings (e.g., =* Header=)
- Horizontal rules (e.g., =-----= or =---=)
- Custom separators via =--separator= flag

-----

* Checklists and Lists
** Technical Task Tracking
- [X] Integrity checks with Go
- [X] High-fidelity Org parsing
- [X] Native checkbox rendering
- [ ] Direct export to PDF
** Nested Structures
- Infrastructure
  - Database
    - Replication
    - Backups

-----

* Tables for Technical Data
| Feature         | Markdown | Org-mode | Search |
|-----------------+----------+----------+--------|
| Format Support  | ✅       | ✅       | Native |
| Highlighting    | ✅       | ✅       | Chroma |
| Theme Palettes  | ✅       | ✅       | Builtin|
| Command Palette | ✅       | ✅       | Flex   |

-----

* Data Modeling with CUE
CUE is powerful for configuration and validation.

#+begin_src cue
#User: {
    id:   int
    name: string
    role: "admin" | "member"
    tags: [...string]
}

myUser: #User & {
    id:   101
    name: "Gopher"
    role: "member"
    tags: ["org-mode", "test"]
}
#+end_src

-----

* System Scripting: Python
Perfect for quick logic and data processing.

#+begin_src python
import os

def list_workspace_files():
    return [f for f in os.listdir('.') if f.endswith('.org')]

print(f"Found Org files: {list_workspace_files()}")
#+end_src

-----

* Power of Rust
Memory safety and performance without a runtime.

#+begin_src rust
#[derive(Debug)]
struct Slide {
    title: String,
    id: u32,
}

fn main() {
    let s = Slide { title: "Rust Intro".into(), id: 42 };
    println!("Rendering: {:?}", s);
}
#+end_src

-----

* Frontend Logic: JavaScript
Modern ESM and interactive features.

#+begin_src javascript
const slideshow = {
    current: 0,
    next() {
        this.current++;
        console.log(`Now at ${this.current}`);
    }
};
slideshow.next();
#+end_src

-----

* Backend Systems: Go
The engine behind Slidesdeck.

#+begin_src go
package main

import "os"

func main() {
    // Elegant system management
    f, _ := os.Create("presentation.html")
    defer f.Close()
}
#+end_src

-----

* Command Palette Search
Press @@html:<kbd>/</kbd>@@ at any time to open the *Search Palette*.

- Powered by *FlexSearch*
- *Orderless Logic*: Search "rust power" to find "Power of Rust"
- Automatic highlighting
- Instant jump to any slide

-----

* Interactive Pause Mode
Manage break times professionally with @@html:<kbd>Shift+P</kbd>@@.

- *Countdown Message*: Customize what's shown during the break
- *Duration*: Choose "Until Time" or "In Minutes"
- Persistent state survives browser reload
- Full-screen high-contrast display

-----

* Keyboard Shortcuts
| Key                                          | Action                |
|----------------------------------------------+-----------------------|
| @@html:<kbd>n</kbd>@@ / @@html:<kbd>→</kbd>@@ | Next Slide            |
| @@html:<kbd>p</kbd>@@ / @@html:<kbd>←</kbd>@@ | Previous Slide        |
| @@html:<kbd>/</kbd>@@                        | Search Palette        |
| @@html:<kbd>t</kbd>@@ / @@html:<kbd>T</kbd>@@ | Theme / Font Palettes |
| @@html:<kbd>f</kbd>@@                        | Toggle Fullscreen     |
| @@html:<kbd>N</kbd>@@                        | Toggle Line Numbers   |
| @@html:<kbd>Shift+P</kbd>@@                  | Pause Mode            |
| @@html:<kbd>?</kbd>@@                        | Help Screen           |

-----

* Go forth and Present!
Slidesdeck - Technical presentations for the modern era.

/Start your next talk with a simple command:/
#+begin_src bash
slidesdeck overview.org
#+end_src
