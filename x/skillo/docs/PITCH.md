# Elevator Pitch

For AI agent developers who need to install/version skills from GitHub
repos, **skillo** is a **Go CLI tool** that **leverages Go modules
(pseudo-versions/tags) to track non-Go assets like SKILL.md**. Unlike
`npx add-skill` or curl scripts, skillo provides a **single
cross-platform binary with native Go dependency management**.
