; Concurrency examples (future: implement goroutines and channels)

; (defn worker [id ch]
;   (println "Worker" id "started")
;   (>! ch (str "Hello from worker " id)))
;
; (defn main []
;   (let [ch (chan string)]
;     (go (worker 1 ch))
;     (go (worker 2 ch))
;     (println (<! ch))
;     (println (<! ch))))

; For now, just compile-time examples
(defn main []
  (println "Concurrency features coming in v0.3"))
